rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /designs/{document=**} {
      allow read: if true;
    }
    match /users/{document=**} {
      allow read: if true;
    }
  }
}
